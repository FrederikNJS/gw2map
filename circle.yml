machine:
  node:
    version: 5.4.1

dependencies:
  cache_directories:
    - "jspm_packages"
    - "node_modules"
  override:
    - npm install -g http-server jspm
    - npm isntall
    - jspm install
    - jspm bundle --inject --minify javascript/gw2map.js javascript/bundle.js
